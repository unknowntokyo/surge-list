function operator(proxies, targetPlatform) {
    const {ALPN} = $arguments;
    proxies.forEach(proxy => {
        if (targetPlatform === "QX") {
            proxy.tfo = `${proxy.tfo || false}, tls-alpn=${ALPN}`;
        }
    });
    return proxies;
}